@model Carvisto.Models.Trip
@{
    ViewData["Title"] = "Delete Trip";
}
<div class="delete-trip-form">
    <div class="delete-trip-card">
    <h2 class="auth-title">Delete Trip</h2>
    <h3 class="delete-title">Are you sure you want to delete this?</h3>
    <div>
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id"/>
            <div class="delete-button-container">
            <button type="submit" class="delete-button-trip">Delete</button>
            <a asp-action="Index" asp-controller="Account" class="back-profile-btn">Back to List</a>
            </div>
        </form>
    </div>
    </div>
</div>