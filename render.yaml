services:
  - type: web
    name: carvisto
    env: docker
    dockerfilePath: ./Dockerfile
    disk:
      name: sqlite-data
      mountPath: /app
      sizeGB: 1
      mountMode: rw
    envVars:
      - key: APP_UID
        value: "1000"
      - key: ConnectionStrings__DefaultConnection
        value: Data Source=/app/Сarvisto.db;Cache=Shared